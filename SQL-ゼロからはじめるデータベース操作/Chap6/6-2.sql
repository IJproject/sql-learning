/*
  述語：戻り値を真偽値とする表現（抽出条件の設定に使える）
  ・LIKE：文字列の部分一致検索（「%」でワイルドカード指定でき、「_」にすると文字数を含めたワイルドカード指定になる）
  ・BETWEEN：範囲指定（「BETWEEN A AND B」は「A以上B以下」と同じ）
  ・IN, NOT IN：リスト指定（「IN (A, B, C)」は「AまたはBまたはC」と同じ）、サブクエリを使うこともできる
*/

-- P.212~
